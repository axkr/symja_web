Insert a keyword and append a `*` to search for keywords in the search field. 

* example input: `Int*`.


Insert the keyword without appending  a `*` to display the documentation of the keyword.

* example input: `FactorInteger`.